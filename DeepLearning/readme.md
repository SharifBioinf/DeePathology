classification-disease.py : The python script to perfoom hyper-parameter optimization in classification of deisease.

classification-tissue.py : The python script to perform hyper-parameter optimization in classification of tissue.

Contractive-AE.py: The python script to create the Contractive Autoencoder.

Contractive-Dropout-AE.py: The python script to create the Contractive Autoencoder with Dropout layers.

Variational-AE.py: The python script to create the Variational Autoencoder.

Variational-Dropout-AE.py: The python script to create the Variational Autoencoder with Dropout layers.

Cross-Val-Contractive-Dropout-CAE.py: The python script to create the Contractive Autoencoder with Dropout layers and cross vlaidation.

hyperopt-CAE: The python script to perfrom hyper-parameters optimization in Contractive Autoencoder model.

hyperopt-Dropout-CAE: The python script to perfrom hyper-parameters optimization in Contractive Autoencoder with Dropout model.

hyperopt-VAE: The python script to perfrom hyper-parameters optimization in Variational Autoencoder model.

hyperopt-Dropout-CAE: The python script to perfrom hyper-parameters optimization in Variational Autoencoder with Dropout model.
